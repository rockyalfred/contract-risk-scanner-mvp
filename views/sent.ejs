<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Result</title>
  <link rel="stylesheet" href="/public/styles.css" />
</head>
<body>
  <div class="container">
    <header class="topbar">
      <div class="brand">
        <h1>Contract Risk Scanner</h1>
        <p><%= (mode === 'copy') ? 'Email text ready' : 'Result sent' %></p>
      </div>
      <div class="kicker">
        <span class="badge ok">Sent</span>
        <span class="badge">Done</span>
      </div>
    </header>

    <main class="content">
      <div class="card">
        <div class="h2">Success</div>
        <% if (mode === 'copy') { %>
          <p class="muted" style="margin-top:6px;">Email sending is disabled in this deployment. Copy the email text below and send it manually to:</p>
          <div class="callout" style="margin-top:12px;"><code><%= to %></code></div>

          <div class="row" style="margin-top:12px;">
            <label>Subject</label>
            <input type="text" value="<%= subject %>" readonly />
          </div>
          <div class="row" style="margin-top:12px;">
            <label>Email body</label>
            <textarea readonly><%= body %></textarea>
          </div>

          <div class="row" style="margin-top:12px;">
            <a class="btn secondary" href="/confirm/<%= id %>/download?to=<%= encodeURIComponent(to) %>&cancelBy=<%= encodeURIComponent(cancelBy || '') %>">Download .txt</a>
            <span class="small" style="margin-left:10px;">Tip: download gives a ready-to-send text file.</span>
          </div>
        <% } else { %>
          <p class="muted" style="margin-top:6px;">Your results were sent to:</p>
          <div class="callout" style="margin-top:12px;"><code><%= to %></code></div>
        <% } %>

        <div class="row" style="margin-top:14px;">
          <a class="btn primary" href="/upload">Scan another PDF</a>
          <a class="btn secondary" href="/" style="margin-left:8px;">Home</a>
        </div>

        <div class="footerBar">
          <div><strong>Privacy</strong><br/>The uploaded PDF was deleted immediately after extraction.</div>
          <div><strong>Accuracy</strong><br/>Store the cancel-by date somewhere you will actually see it.</div>
          <div><strong>Support</strong><br/>contractrisk.team@gmail.com</div>
        </div>
      </div>
    </main>
  </div>
</body>
</html>
