<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Upload</title>
  <link rel="stylesheet" href="/public/styles.css" />
</head>
<body>
  <div class="container">
      <header class="topbar">
      <div class="brand">
        <h1>Contract Risk Scanner</h1>
        <p>Upload a PDF to scan renewal and notice terms</p>
      </div>
      <div class="kicker">
        <span class="badge">Step 2 of 3</span>
        <span class="badge">Upload</span>
        <span class="badge ok">PDF up to 10MB</span>
      </div>
    </header>

    <main class="content">
      <div class="card">
        <div class="grid" style="gap:12px;">
          <div>
            <div class="h2">Upload contract PDF</div>
            <p class="muted" style="margin-top:6px;">We extract evidence snippets and compute a cancel-by date when possible. You review everything before sending.</p>

            <% if (error) { %>
              <div class="row error"><%= error %></div>
            <% } %>

            <form method="post" action="/upload" enctype="multipart/form-data" style="margin-top:14px;" id="uploadForm">
              <input type="hidden" name="_csrf" value="<%= csrfToken || '' %>" />
              <div class="drop">
                <label>Select a PDF file</label>
                <input type="file" name="pdf" accept="application/pdf" required id="pdfInput"/>
                <div class="small" style="margin-top:10px;">Tip: use a clean PDF export. Scanned images may reduce accuracy.</div>
              </div>

              <div class="row" id="uploadProgress" style="display:none;">
                <div style="background: #f0f0f0; border-radius: 8px; padding: 20px; text-align: center;">
                  <div style="font-size: 32px; margin-bottom: 10px;">⏳</div>
                  <div style="font-size: 18px; font-weight: bold; color: #333;">Uploading your contract...</div>
                  <div style="font-size: 14px; color: #666; margin-top: 8px;">Please wait while we process your file</div>
                </div>
              </div>

              <div class="row" id="uploadSuccess" style="display:none;">
                <div style="background: #d4edda; border: 1px solid #c3e6cb; border-radius: 8px; padding: 20px; text-align: center;">
                  <div style="font-size: 32px; margin-bottom: 10px;">✅</div>
                  <div style="font-size: 18px; font-weight: bold; color: #155724;">Upload successful!</div>
                  <div style="font-size: 14px; color: #155724; margin-top: 8px;">Analyzing your contract now...</div>
                </div>
              </div>

              <div class="row" id="uploadButtons">
                <button class="btn primary" type="submit">Scan</button>
                <a class="btn secondary" href="/" style="margin-left:8px;">Back</a>
              </div>
            </form>

          </div>

          <div class="callout info">
            <div class="h2">What gets extracted</div>
            <ul class="list">
              <li>Renewal or end date evidence</li>
              <li>Notice period evidence for non-renewal</li>
              <li>Rolling monthly detection (when relevant)</li>
            </ul>
            <div class="small" style="margin-top:10px;">
              AI disclosure: if extraction is uncertain, we may use AI on short, redacted snippets (not the full contract). You must verify before sending.
            </div>
          </div>
        </div>

        <div class="footerBar">
          <div><strong>Privacy</strong><br/>The uploaded PDF is deleted immediately after extraction.</div>
          <div><strong>Accuracy</strong><br/>Scanned image PDFs may reduce extraction accuracy.</div>
          <div><strong>Support</strong><br/>contractrisk.team@gmail.com</div>
        </div>
      </div>
    </main>
  </div>

  <script>
    (function () {
      var form = document.getElementById('uploadForm');
      var pdfInput = document.getElementById('pdfInput');
      var uploadProgress = document.getElementById('uploadProgress');
      var uploadSuccess = document.getElementById('uploadSuccess');
      var uploadButtons = document.getElementById('uploadButtons');
      
      if (!form) return;

      form.addEventListener('submit', function(e) {
        // Check if file is selected
        if (!pdfInput.files || !pdfInput.files[0]) {
          return;
        }
        
        // Show progress indicator
        uploadButtons.style.display = 'none';
        uploadProgress.style.display = 'block';
        
        // Simulate success after a short delay (actual upload continues)
        setTimeout(function() {
          uploadProgress.style.display = 'none';
          uploadSuccess.style.display = 'block';
        }, 800);
      });
    })();
  </script>
</body>
</html>
